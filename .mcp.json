{
  "mcpServers": {
    "power-automate": {
      "command": "node",
      "args": ["dist/bin.js", "--stdio"],
      "env": {
        "AZURE_CLIENT_ID": "${MS365_CLIENT_ID}",
        "AZURE_TENANT_ID": "common"
      }
    }
  }
}
